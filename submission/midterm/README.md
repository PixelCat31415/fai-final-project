All files are runtime dependencies of the two agents.

- `customized_engine.py` -- a simplified engine implementation for internally simulating action outcomes
- `monte_carlo.py` -- game strategies logic and the actual player classes definition
- `best_player1.py`, `best_player2.py` -- proxy to the players defined in `monte_carlo.py`
